.container-fluid
  - flash.each do |name, msg|
    - if msg.is_a? Array
      - msg.each do |m|
        .row
          = render 'shared/flash', message: m, name: name
    - else
      = render 'shared/flash', message: msg, name: name